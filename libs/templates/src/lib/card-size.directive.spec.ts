import { CardSizeComponent } from './card-size.directive';

describe('CardSizeComponent', () => {
  it('should create an instance', () => {
    const directive = new CardSizeComponent();
    expect(directive).toBeTruthy();
  });
});
